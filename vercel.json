{
  "buildCommand": "cd coinlens && npm install && npm run build",
  "outputDirectory": "coinlens/dist",
  "installCommand": "echo 'skip root install'",
  "functions": {
    "api/index.py": {
      "maxDuration": 60,
      "includeFiles": "backend/*.py"
    }
  },
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/api/index.py"
    }
  ]
}
